apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-test.fullname" . }}-test-connection"
  labels:
    {{- include "api-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
